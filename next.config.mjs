/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    basePath: '/horarios',
    output: 'export',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
